extends layout

block content
  - var posts = _(contents.posts).chain().toArray().first(5).value()
  - each post in posts
    article
      a(href=post.url)
        h1= post.title
      block posthtml
        != post.html
        - if (post.hasMore)
          a.pull-right(href=post.url) Read More
